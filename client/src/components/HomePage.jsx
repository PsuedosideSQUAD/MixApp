import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';


const HomePage = () => (
  <Card className="container">
    <CardTitle title="Mix It" subtitle="Home Page." />
  </Card>
);

export default HomePage;
